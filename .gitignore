# Built binary (repo root)
/nshunter
*.exe

# Local runs / scratch data dirs (Pebble, SQLite, captures)
tmpdata/
tmpdata-*/

# Go
*.test
*.prof
coverage.out

# OS / editor
.DS_Store

# Debian package build (dpkg-buildpackage / debuild)
debian/files
debian/debhelper-build-stamp
debian/*.substvars
debian/*.debhelper
debian/*.debhelper.log
debian/nshunter/
obj-x86_64-linux-gnu/
